
import AuthPlugin                   from '../plugins/auth.plugin'
import {
    PluginsConfig
} from '../config'

const {
    routerConfig,
} = PluginsConfig.ROUTER;

export default {

    data: {
        auth$: {},
    },

    authGet ()  {

    },
}

<!--index.wxml-->
<view class="cell">
    <view wx:if="{{iconClass}}" class="icon {{iconClass}}"></view>
    <text class="label">{{cellLabel}}</text>
    <text class="value">{{cellValue}}</text>
</view>

import './index.json'
import './index.wxml'
import './index.scss'

import WowComponent from 'source/lib/component'

new WowComponent({
    // 外部样式类
    externalClasses: [
        'class-navigation',
    ],
});

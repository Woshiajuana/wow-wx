<!--index.wxml-->
<wow-cell
    data-url="{{item.url}}"
    wx:for="{{arrEntry}}"
    wx:key="index"
    cellLabel="{{item.label}}"
    useMargin="{{item.useMargin}}"
    bindtap="jumpPageOrFireFn"
    data-item="{{item}}"
></wow-cell>
